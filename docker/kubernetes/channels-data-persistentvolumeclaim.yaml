apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: channels-data
  name: channels-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100G
